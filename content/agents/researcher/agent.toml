name = "researcher"
description = "Reads docs/source and reports back facts; marks anything it can't verify as unverified"
model_tier = "standard"
tools = ["read", "grep", "find", "bash"]

[claude]
color = "yellow"

[pi]
provider = "codex"
